{
  "private": true,
  "scripts": {
    "build": "rm -rf dist && esbuild js/player-bootstrap.js --bundle --format=esm --splitting --outdir=dist --minify"
  },
  "dependencies": {
    "ioredis": "^5.10.0",
    "ios-haptics": "^0.1.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
