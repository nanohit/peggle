{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/*.js && node --check scripts/*.mjs && node --check js/main.js && node --check js/player-bootstrap.js && node --check js/image-compression.js && node --check js/visual-layout.js && node --check js/character-config.js"
  },
  "dependencies": {
    "ioredis": "^5.10.0",
    "ios-haptics": "^0.1.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
